'use strict';

var should = require('should');
var app = require('../../app');
var request = require('supertest');
var Statistics = require('./statistics.service');


describe('Statistics Tests', function() {
  it('should send error if user does not exist');
});